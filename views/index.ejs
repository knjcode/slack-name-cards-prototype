<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title><%= team_name %> slack cards</title>
    <link rel="stylesheet" type="text/css" href="//fonts.googleapis.com/css?family=Lato" />
    <style>
      body {
        font-family: Lato;
      }
      .wrapper {
        display:          flex;
        flex-wrap:        wrap;
        align-content:    flex-start;
        width:            210mm;
        height:           297mm;
        page-break-after: always;
      }
      .wrapper:last-child {
        page-break-after: auto;
      }
      .card {
        width:    91mm;
        height:   55mm;
        border:   #000000 solid 1px;
        overflow: hidden;
      }
      .usericon {
        position:      relative;
        width:         192px;
        height:        192px;
        margin-top:    8px;
        margin-left:   8px;
        border-radius: .2rem;
      }
      .username {
        position:      absolute;
        width:         130px;
        margin-top:    26px;
        margin-left:   10px;
        font-weight:   bold;
        white-space:   nowrap;
        text-overflow: ellipsis;
        overflow:      hidden;
      }
      .realname {
        position:    absolute;
        width:       128px;
        margin-top:  72px;
        margin-left: 10px;
      }
      .teamicon {
        position:     relative;
        width:        50px;
        height:       50px;
        margin-top:   80px;
        margin-left:  82px;
        border-radius: .25rem;
      }
    </style>
  </head>
  <body>
    <% usersArray.forEach((users) => { %>
      <div class="wrapper">
        <% users.forEach((user) => { %>
          <div class="card">
            <img class="usericon" src="<%= user.icon %>" />
            <span class="username"><%= user.name %></span>
            <span class="realname"><%= user.real_name %></span>
            <img class="teamicon" src="<%= team_icon %>" />
          </div>
        <% }) %>
      </div>
    <% }) %>
  </div>
  </body>
</html>
